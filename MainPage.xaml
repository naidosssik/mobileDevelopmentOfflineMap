<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mapsui="clr-namespace:Mapsui.UI.Maui;assembly=Mapsui.UI.Maui"
    x:Class="OfflineMapApp.MainPage"
    Title="Карта">

    <ContentPage.MenuBarItems>

        <MenuBarItem Text="Файл">

            <MenuFlyoutItem
                Text="Открыть фото"
                Clicked="OpenPhotoMenu_Clicked">

                <MenuFlyoutItem.KeyboardAccelerators>

                    <KeyboardAccelerator
                        Modifiers="Ctrl"
                        Key="O" />

                </MenuFlyoutItem.KeyboardAccelerators>

            </MenuFlyoutItem>

        </MenuBarItem>

    </ContentPage.MenuBarItems>

    <Grid
        RowDefinitions="Auto,*,Auto"
        ColumnDefinitions="*,300">

        <!-- Верхняя панель -->
        <Grid
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Padding="20,15"
            ColumnDefinitions="*,Auto">

            <Label
                Text="Карта с точками и оффлайн-режим"
                FontSize="24"
                FontAttributes="Bold"
                VerticalOptions="Center" />

        </Grid>

        <!-- Карта -->
        <Border
            Grid.Row="1"
            Grid.Column="0"
            Margin="10"
            StrokeThickness="1">

            <Grid>

                <!-- Карта -->
                <mapsui:MapControl
                    x:Name="MapView"
                    HorizontalOptions="Fill"
                    VerticalOptions="Fill" />

                <!-- Кнопки масштабирования -->
                <VerticalStackLayout
                    HorizontalOptions="End"
                    VerticalOptions="End"
                    Margin="0,0,20,20"
                    Spacing="8">

                    <Button
                        Text="+"
                        WidthRequest="50"
                        HeightRequest="50"
                        FontSize="24"
                        FontAttributes="Bold"
                        Clicked="ZoomIn_Clicked" />

                    <Button
                        Text="−"
                        WidthRequest="50"
                        HeightRequest="50"
                        FontSize="24"
                        FontAttributes="Bold"
                        Clicked="ZoomOut_Clicked" />

                </VerticalStackLayout>

            </Grid>

        </Border>

        <!-- Правая панель -->
        <VerticalStackLayout
            Grid.Row="1"
            Grid.Column="1"
            Padding="15"
            Spacing="12">

            <Label
                Text="Мои точки"
                FontSize="20"
                FontAttributes="Bold" />

            <Label
                x:Name="EmptyPointsLabel"
                Text="Пока точек нет"
                FontSize="14"
                TextColor="Gray" />

            <CollectionView
                x:Name="PointsCollectionView"
                HeightRequest="300"
                SelectionMode="Single"
                SelectionChanged="PointsCollectionView_SelectionChanged">

                <CollectionView.ItemTemplate>

                    <DataTemplate>

                        <Border
                            Stroke="#DDDDDD"
                            StrokeThickness="1"
                            Padding="10"
                            Margin="0,0,0,8">

                            <Grid
                                RowDefinitions="Auto,Auto"
                                ColumnDefinitions="Auto,*"
                                ColumnSpacing="10"
                                RowSpacing="8">

                                <!-- Маркер -->
                                <Label
                                    Text="●"
                                    FontSize="18"
                                    VerticalOptions="Center" />

                                <!-- Название и координаты -->
                                <VerticalStackLayout
                                    Grid.Column="1"
                                    Spacing="2">

                                    <Label
                                        Text="{Binding Name}"
                                        FontAttributes="Bold"
                                        FontSize="15" />

                                    <Label
                                        Text="{Binding CoordinatesText}"
                                        FontSize="12"
                                        TextColor="Gray" />

                                    <Label
                                        Text="{Binding Description}"
                                        FontSize="12"
                                        TextColor="Gray"
                                        LineBreakMode="TailTruncation"
                                        MaxLines="2" />

                                </VerticalStackLayout>

                                <Grid
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    RowDefinitions="Auto,Auto"
                                    ColumnDefinitions="*,*"
                                    RowSpacing="8"
                                    ColumnSpacing="8">

                                    <Button
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Text="Перейти"
                                        FontSize="12"
                                        Padding="8,5"
                                        CommandParameter="{Binding .}"
                                        Clicked="GoToPoint_Clicked" />

                                    <Button
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        Text="Маршрут"
                                        FontSize="12"
                                        Padding="8,5"
                                        CommandParameter="{Binding .}"
                                        Clicked="BuildRoute_Clicked" />

                                    <Button
                                        Grid.Row="1"
                                        Grid.Column="0"
                                        Text="Изменить"
                                        FontSize="12"
                                        Padding="8,5"
                                        CommandParameter="{Binding .}"
                                        Clicked="EditPoint_Clicked" />

                                    <Button
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Text="Удалить"
                                        FontSize="12"
                                        Padding="8,5"
                                        CommandParameter="{Binding .}"
                                        Clicked="DeletePoint_Clicked" />

                                </Grid>

                            </Grid>

                        </Border>

                    </DataTemplate>

                </CollectionView.ItemTemplate>

            </CollectionView>

            <Button
                Text="+ Добавить точку"
                Clicked="AddPoint_Clicked" />

            <Button
                Text="Моё местоположение"
                Clicked="MyLocation_Clicked" />

            <Button
                x:Name="OfflineModeButton"
                Text="Оффлайн-режим"
                Clicked="OfflineMode_Clicked" />

            <Button
                Text="Скачать область"
                Clicked="DownloadArea_Clicked" />

            <Label
                x:Name="OfflineStatusLabel"
                Text="Режим: онлайн"
                FontSize="12"
                TextColor="Gray" />

        </VerticalStackLayout>

        <!-- Нижняя строка -->
        <Grid
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Padding="20,10"
            ColumnDefinitions="*,*">

            <Label
                x:Name="GpsStatusLabel"
                Text="GPS: не определён" />

        </Grid>

    </Grid>

</ContentPage>